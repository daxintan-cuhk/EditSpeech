The proposed system is to synthesize by partial inference from the inserted text with original audio context. The baseline_1 system is to synthesized directly from the inserted text.The baseline 2 system is to synthesized from the text to insert and concatenated to the original audio.The baseline 3 system is to synthesized from the inserted text, and clipped and concatenated to the original audio